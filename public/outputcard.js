  /*  
    // Create a new card
      var card = document.createElement('div');
      card.className = 'card';
      card.style = 'width: 18rem;';

      // Create a card image
      var img = document.createElement('img');
      img.className = 'card-img-top';
      img.src = 'https://via.placeholder.com/286x180';

      // Create a card body
      var cardBody = document.createElement('div');
      cardBody.className = 'card-body';

      // Create a card title
      var title = document.createElement('h5');
      title.className = 'card-title';
      title.textContent = 'Bolt KES';

      // Create a card text
      var text = document.createElement('p');
      text.className = 'card-text';
      text.textContent = 'Bolt Price';

      // Append the image, title and text to the card body
      cardBody.appendChild(title);
      cardBody.appendChild(text);

      // Append the image and card body to the card
      card.appendChild(img);
      card.appendChild(cardBody);

      // Append the card to the output div
      var output = document.getElementById('output1');
      output1.appendChild(card);

      */